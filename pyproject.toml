[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "congressmonitor"
version = "0.1.0"
description = "Command-line tool for monitoring U.S. Congressional stock trade disclosures."
authors = [
    {name = "Generated by ChatGPT"}
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
congressmonitor = "congressmonitor.cli:main"
